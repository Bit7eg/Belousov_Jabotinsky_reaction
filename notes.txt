Настроить тесты для каждой функции:
    1) сходимость всех методов к решению (посчитать, нарисовать)
    2) сходимость в себе (посчитать, нарисовать)
    3) влияние метода для предвычислений на методы в них нуждающиеся (запариться с автоматизацией?)
    4) определение оптимального параметра для итераций (возможно стоит написать отдельный скрипт)
    5) фактический порядок сходимости методов (посчитать)